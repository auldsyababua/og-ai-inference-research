# Snowflake Connection Configuration
# This file should be kept secure and not committed to git
# For Streamlit Community Cloud, add these as secrets in the app settings
#
# Using key-pair authentication (required for MFA-enabled accounts)

[connections.snowflake]
account = "ZWZLXDA-MEB82135"
user = "10NETZERO"
private_key_file = "rsa_key.pem"
role = "ACCOUNTADMIN"
warehouse = "CALCULATOR_WH"  # Created and verified
database = "CALCULATOR_DB"   # Created and verified
schema = "PUBLIC"            # Created and verified

